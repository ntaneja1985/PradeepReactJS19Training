export type Product = {
    productId: number,
    productName: string,
    productPrice: number,
    productCode: string,
    category: number
}

export type Category = {
    id: number,
    categoryName: string,
}